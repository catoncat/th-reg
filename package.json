{
  "name": "tokenharbor-register",
  "version": "0.1.0",
  "description": "Pure-HTTP registration for Token Harbor (tokenharbor.ai): no browser, no proxy required. React-19 server action replay + real mailbox verification (mandatory — API returns 403 without it).",
  "type": "module",
  "bin": {
    "th-register": "src/cli.mjs",
    "th": "src/th.mjs"
  },
  "scripts": {
    "register": "node src/cli.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
