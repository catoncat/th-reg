{
  "name": "tokenharbor-register",
  "version": "0.1.0",
  "description": "Pure-HTTP registration for Token Harbor (tokenharbor.ai): no browser, no proxy/mail required (both optional). React-19 server action replay + Supabase auto-confirm verification.",
  "type": "module",
  "bin": {
    "th-register": "src/cli.mjs"
  },
  "scripts": {
    "register": "node src/cli.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
